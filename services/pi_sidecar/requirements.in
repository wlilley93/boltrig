# Direct runtime dependencies for the standalone Pi sidecar.
#
# Regenerate requirements.txt with CPython 3.12 so environment markers and
# wheels match the production image:
#   python -m piptools compile --generate-hashes --resolver=backtracking \
#     --output-file=requirements.txt requirements.in
fastapi==0.139.0
httpx==0.28.1
uvicorn[standard]==0.51.0
