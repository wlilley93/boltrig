{
  "version": 1,
  "_comment": [
    "Advisories accepted under docs/dependency-policy.md 'Required verification' item 6:",
    "every entry carries reachability, owner, expiry and a compensating control, and an",
    "EXPIRED entry fails the audit rather than silently continuing to suppress. Accept an",
    "advisory ONLY when there is no fixed upstream release; if a fix exists, take the fix.",
    "Enforced by scripts/python_audit.py, invoked from `make python-audit`."
  ],
  "accepted": [
    {
      "id": "PYSEC-2026-2447",
      "package": "diskcache",
      "version": "5.6.3",
      "owner": "memory-maintainers",
      "expires": "2026-10-23",
      "no_fix_available": true,
      "reachability": "Transitive, via cognee only (diskcache is not a direct dependency). The advisory is arbitrary code execution through DiskCache's default pickle serialization, and it requires WRITE access to the cache directory. Boltrig gives cognee a dedicated named volume at BOLTRIG_COGNEE_ROOT (/var/lib/boltrig/cognee) inside a container whose root filesystem is read_only, and nothing but the kernel process writes there. The cache directory is never shared between tenants and is never attacker-writable over any request path, so an actor able to exploit this already holds code execution inside the container.",
      "compensating_control": "Dedicated per-stack named volume (not a shared or world-writable path); read_only root filesystem with only /tmp writable (x-app-hardening); the knowledge subsystem is default-OFF and only reaches cognee when a manifest explicitly enables it.",
      "reason": "No fixed release exists upstream: pip-audit reports the advisory with an empty fix-versions list. Re-check at expiry and take the fix the moment python-diskcache publishes one.",
      "ecosystem": "python"
    },
    {
      "id": "GHSA-mh99-v99m-4gvg",
      "package": "brace-expansion",
      "version": "1.1.16, 2.1.2",
      "owner": "site-maintainers",
      "expires": "2026-10-23",
      "no_fix_available": true,
      "reachability": "Build/lint tooling only, never the shipped site bundle. The 1.x and 2.x lines reach the tree solely through minimatch@3 under eslint and @vitest/coverage-v8. The advisory is a denial of service via unbounded brace expansion, which requires processing an attacker-controlled glob pattern; the only patterns fed to these tools are authored in-repo (eslint config and test globs), so no served request can reach it.",
      "compensating_control": "The 5.x line IS pinned to the fixed 5.0.8 via a per-major override, so every dependency that can take the fix has it. Only the two legacy lines, which have no fixed release, remain.",
      "reason": "No fixed release exists on the 1.x or 2.x lines: 1.x ends at 1.1.16 and 2.x at 2.1.2, and the fix ships only in 5.0.8, whose API minimatch@3 cannot use (forcing it throws at lint time). Closing this needs the PULLERS to move off minimatch@3, not a version pin. Re-check at expiry.",
      "ecosystem": "npm"
    },
    {
      "id": "GHSA-hrxh-6v49-42gf",
      "package": "google.golang.org/grpc",
      "version": "1.80.0",
      "owner": "release-engineering",
      "expires": "2026-10-23",
      "no_fix_available": true,
      "reachability": "Vendored inside the rclone binary copied from the digest-pinned official image into the backup sidecar. The advisory covers xDS RBAC and HTTP/2 handling in gRPC servers; the backup image runs rclone strictly as a CLIENT in a fixed backup loop, serves no gRPC, publishes no ports, and never loads xDS service-mesh configuration, so no server-side path the advisory covers exists in this deployment.",
      "compensating_control": "paths-scoped acceptance in .trivyignore.yaml (usr/local/bin/rclone only, so the finding still fires anywhere else); trivy's expired_at returns the finding to the release gate past 2026-10-23; this ledger fails `make python-audit` on expiry; the backup service is profile-gated, publishes no ports, and mounts the rclone config read-only.",
      "reason": "No fixed release exists upstream: fixed in grpc 1.82.1, but rclone v1.74.4 IS the latest upstream release and carries 1.80.0, so there is nothing to upgrade to. Drop this the moment rclone ships a build with grpc >= 1.82.1 (2026-VJS-CC-BOLTRIG-SUPPLY-CHAIN-ADVISORY-ACCEPTANCE-001, D2 - completing the record the .trivyignore.yaml statement already claimed).",
      "ecosystem": "go"
    },
    {
      "id": "CVE-2026-56852",
      "package": "golang.org/x/text",
      "version": "0.38.0",
      "owner": "release-engineering",
      "expires": "2026-10-23",
      "no_fix_available": true,
      "reachability": "Vendored inside the rclone binary copied from the digest-pinned official image into the backup sidecar. The advisory is a norm.Iter infinite loop on input containing invalid UTF-8. The backup image runs rclone strictly as a CLIENT in a fixed backup loop against operator-configured remotes; it serves nothing and publishes no ports, but it DOES process names and listings returned by the remote, so reaching the defect requires a compromised operator-provisioned remote - at which point the attacker already holds the off-box backup channel (2026-VJS-CC-BOLTRIG-SUPPLY-CHAIN-ADVISORY-ACCEPTANCE-001, F5 as qualified).",
      "compensating_control": "paths-scoped acceptance in .trivyignore.yaml (usr/local/bin/rclone only, so the finding still fires anywhere else); trivy's expired_at returns the finding to the release gate past 2026-10-23; this ledger fails `make python-audit` on expiry; the backup service is profile-gated, publishes no ports, and mounts the rclone config read-only.",
      "reason": "No fixed release exists upstream: fixed in x/text 0.39.0, but rclone v1.74.4 IS the latest upstream release and pins 0.38.0 (master has 0.40.0, unreleased), so there is nothing to upgrade to. Drop this the moment a released rclone carries golang.org/x/text >= 0.39.0 (2026-VJS-CC-BOLTRIG-SUPPLY-CHAIN-ADVISORY-ACCEPTANCE-001).",
      "ecosystem": "go"
    },
    {
      "id": "CVE-2026-16804",
      "package": "chromium",
      "version": "150.0.7871.181-1~deb12u1",
      "owner": "fleet-maintainers",
      "expires": "2026-09-11",
      "no_fix_available": true,
      "reachability": "NOT UNREACHABLE, and stated so deliberately. chromium runs in both production fleet workers and the browser-cli adapter is ACTIVATED on the Classical Visas tenant; browser automation renders remote content by definition, so a sandbox escape is live wherever a browser verb is invoked. What bounds it TODAY is usage, not architecture: all six verbs (web.fetch, browser.doctor, browser.tab.open, browser.remote.start, browser.remote.stop, browser.page.info) have ZERO rows in that tenant's audit_log - never once invoked - and the second tenant registers no browser adapter at all. Usage is a weak bound because it changes the day someone calls a verb, with no code or config change, which is why the expiry is six weeks rather than the usual year. Prefer deactivating browser-cli where unused, or dropping chromium from the image, over renewing this.",
      "note": "sandbox escape via crafted HTML. Trivy reports fixed 151.0.7922.47-1, which is the UPSTREAM CHROME version, not a Debian package. Measured 2026-07-31: bookworm and bookworm-security both offer exactly 150.0.7871.181-1~deb12u1, and trixie the same code as -1~deb13u1, so no Debian release carries the fix and no rebuild or base change takes it. Follows the ratio of [2026] VJS-CC-BOLTRIG-SUPPLY-CHAIN-ADVISORY-ACCEPTANCE-001. Surfaced only because PR #198 was the first change today to go through a pull request rather than a direct push with branch protection bypassed."
    },
    {
      "id": "CVE-2026-16805",
      "package": "chromium",
      "version": "150.0.7871.181-1~deb12u1",
      "owner": "fleet-maintainers",
      "expires": "2026-09-11",
      "no_fix_available": true,
      "reachability": "NOT UNREACHABLE, and stated so deliberately. chromium runs in both production fleet workers and the browser-cli adapter is ACTIVATED on the Classical Visas tenant; browser automation renders remote content by definition, so a sandbox escape is live wherever a browser verb is invoked. What bounds it TODAY is usage, not architecture: all six verbs (web.fetch, browser.doctor, browser.tab.open, browser.remote.start, browser.remote.stop, browser.page.info) have ZERO rows in that tenant's audit_log - never once invoked - and the second tenant registers no browser adapter at all. Usage is a weak bound because it changes the day someone calls a verb, with no code or config change, which is why the expiry is six weeks rather than the usual year. Prefer deactivating browser-cli where unused, or dropping chromium from the image, over renewing this.",
      "note": "use-after-free in Blink. Trivy reports fixed 151.0.7922.47-1, which is the UPSTREAM CHROME version, not a Debian package. Measured 2026-07-31: bookworm and bookworm-security both offer exactly 150.0.7871.181-1~deb12u1, and trixie the same code as -1~deb13u1, so no Debian release carries the fix and no rebuild or base change takes it. Follows the ratio of [2026] VJS-CC-BOLTRIG-SUPPLY-CHAIN-ADVISORY-ACCEPTANCE-001. Surfaced only because PR #198 was the first change today to go through a pull request rather than a direct push with branch protection bypassed."
    },
    {
      "id": "CVE-2026-16806",
      "package": "chromium",
      "version": "150.0.7871.181-1~deb12u1",
      "owner": "fleet-maintainers",
      "expires": "2026-09-11",
      "no_fix_available": true,
      "reachability": "NOT UNREACHABLE, and stated so deliberately. chromium runs in both production fleet workers and the browser-cli adapter is ACTIVATED on the Classical Visas tenant; browser automation renders remote content by definition, so a sandbox escape is live wherever a browser verb is invoked. What bounds it TODAY is usage, not architecture: all six verbs (web.fetch, browser.doctor, browser.tab.open, browser.remote.start, browser.remote.stop, browser.page.info) have ZERO rows in that tenant's audit_log - never once invoked - and the second tenant registers no browser adapter at all. Usage is a weak bound because it changes the day someone calls a verb, with no code or config change, which is why the expiry is six weeks rather than the usual year. Prefer deactivating browser-cli where unused, or dropping chromium from the image, over renewing this.",
      "note": "arbitrary code execution via use-after-free. Trivy reports fixed 151.0.7922.47-1, which is the UPSTREAM CHROME version, not a Debian package. Measured 2026-07-31: bookworm and bookworm-security both offer exactly 150.0.7871.181-1~deb12u1, and trixie the same code as -1~deb13u1, so no Debian release carries the fix and no rebuild or base change takes it. Follows the ratio of [2026] VJS-CC-BOLTRIG-SUPPLY-CHAIN-ADVISORY-ACCEPTANCE-001. Surfaced only because PR #198 was the first change today to go through a pull request rather than a direct push with branch protection bypassed."
    },
    {
      "id": "CVE-2026-16807",
      "package": "chromium",
      "version": "150.0.7871.181-1~deb12u1",
      "owner": "fleet-maintainers",
      "expires": "2026-09-11",
      "no_fix_available": true,
      "reachability": "NOT UNREACHABLE, and stated so deliberately. chromium runs in both production fleet workers and the browser-cli adapter is ACTIVATED on the Classical Visas tenant; browser automation renders remote content by definition, so a sandbox escape is live wherever a browser verb is invoked. What bounds it TODAY is usage, not architecture: all six verbs (web.fetch, browser.doctor, browser.tab.open, browser.remote.start, browser.remote.stop, browser.page.info) have ZERO rows in that tenant's audit_log - never once invoked - and the second tenant registers no browser adapter at all. Usage is a weak bound because it changes the day someone calls a verb, with no code or config change, which is why the expiry is six weeks rather than the usual year. Prefer deactivating browser-cli where unused, or dropping chromium from the image, over renewing this.",
      "note": "sandbox escape (Codecs). Trivy reports fixed 151.0.7922.47-1, which is the UPSTREAM CHROME version, not a Debian package. Measured 2026-07-31: bookworm and bookworm-security both offer exactly 150.0.7871.181-1~deb12u1, and trixie the same code as -1~deb13u1, so no Debian release carries the fix and no rebuild or base change takes it. Follows the ratio of [2026] VJS-CC-BOLTRIG-SUPPLY-CHAIN-ADVISORY-ACCEPTANCE-001. Surfaced only because PR #198 was the first change today to go through a pull request rather than a direct push with branch protection bypassed."
    }
  ]
}
