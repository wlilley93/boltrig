# Boltrig handover — 2026-07-23 (codex-only + Opbox emplacement session)

Scope: everything from the structural sweep through codex-only cutover and the
Opbox agent replacement. For the earlier state see `HANDOVER-2026-07-21.md`.

## What this session shipped (all on main unless noted)

Structural sweep (7 commits: `dae4f2c`..`114b1cc`): ~90 findings fixed —
credential sealing at rest (SEC-169, `BOLTRIG_SEAL_KEY`), kernel→fleet boundary
enforced, reserved-key context stripping, channel grant ceilings + escalation
clamps, Mem/PG store parity, CAS transitions, cost-reservation governance,
workflow durability+checkpoint resume, bounded observability reads, legacy
runtime lanes gated behind `BOLTRIG_ENABLE_LEGACY_RUNTIMES`.

Channel gateway Phase 2 complete: `services/channel_gateway` (Slack, Telegram,
Discord, Signal, WhatsApp/Baileys, generic JSON-lines) — durable dedup
(SEC-175), durable outbox CAS (SEC-176), run-scoped token links (SEC-177),
tier-1/2 addressing honored by the pump (SEC-178), notification round-trip to
bound surfaces incl. department prefs (SEC-179), self-serve onboarding
(SEC-180). `boltrig chat` terminal CLI (head + gateway modes). `workflow:`
targets. HITL trio: approver-only `comment` on channel.send, channel HITL
replies (`/approve` `/deny` `/answer`), SEC-181 secure input (sealed
run+purpose-scoped refs, swept at settle).

Opbox emplacement (`ee0b3ef`, `77eb06d`, `b117352`, `5fa979b`, `895203d` +
opbox-prod commits `69b8c3a2`, `e1eb8ae6`, `ffa09640`, `6664586`):
- Boltrig consumes the Opbox kernel MCP door live: 633 verbs published as
  `opbox.<tool>` (namespaced), reads execute, writes HITL-gate via
  riskClass→consequence mapping. Proof: matter 1003 created on the real kernel.
- Opbox per-bearer tier ceiling fixed (their `69b8c3a2`): 143→634 tools.
- `sdks/node` boltrig-app-sdk (server scaffold + registration + SSE/HITL head).
- Frontend repoint (`ffa09640`): demo frontend streams boltrig SSE end-to-end;
  opbox-agent runtime **retired** (container stopped, compose service removed
  with decision log LOG-2026-07-23-121455).
- Voice surface (SEC-183): `voice.speak/listen/voices.list` adapter + gateway
  realtime adapter (function-tools-only, all calls via run-scoped MCP).
- SEC-182 sole-author four-eyes exemption; HITL timeout enforcement (TIMED_OUT
  + janitor sweep); adapter lifecycle verbs (deactivate/delete) + boot/on-demand
  rehydration; StreamableHTTP MCP consumer; operator-vetted `allow_internal`
  egress waiver; SEC-184 codex kernel-tools lane; SEC-185 session-auth codex
  posture (Gap 6, decision 0017).

## Current serving state

- Boltrig stack: kernel + fleet-worker + postgres + redis + hatchet + bifrost +
  ui healthy; `readyz: ready`. **Pi is out of the serving path** (container
  removed; compose service profile-gated to `legacy`). All manifest runtime
  lanes are `runtime: codex` (in untracked local `manifest.yaml`).
- Opbox demo: kernel on `tier-proof` image (tier-store + 0125 fix), frontend on
  `boltrig-chat` image streaming boltrig chat. `Opbox-Agent-Runtime` stopped.
- Suite: **2295 passed / 172 skipped**, invariants debt 0, ruff/architecture/
  structure gates green.

## Open issues (priority order)

1. **`BOLTRIG_CODEX_MCP_RUN_TOKEN` never reaches the spawned cell's
   environment.** Verified directly on fresh cells (token_env=0). Config entry
   `[mcp_servers.boltrig]` DOES render (url + bearer env var name), so
   `kernel_scope` is non-None at `_write_cell_config`; the env helper
   `_kernel_tools_environment` (codex_trusted_proxy_provider.py:141) is proven
   correct in isolation, as is `validated_environment_additions`. Suspect is
   the take at `codex_trusted_proxy_provider.py:270` returning None in the live
   path (provider/registry instance or assignment-id mismatch), or the env
   being dropped between `supervisor.start(environment_additions=...)` and
   `os.execve` (cell_spawner.py:367). Effect: cell's MCP `tools/list` 401s
   silently, `thread_dynamic_tools` empty, model answers bare.
2. **`codex_empty_output`**: codex turns intermittently return empty from the
   cell's model call (degrade reason). Possibly related to #1 or to the
   per-cell model bearer/bifrost route.
3. **Admission mismatch seen earlier** ("observed MCP inventory does not match
   the admitted lane", codex_runtime_admission.py:234): occurred when the chat
   turn had no effective tools and fell back read-only; root cause of empty
   grants was `skills_by_role` (fixed — see below). If it recurs with a live
   tool set, re-open.
4. **Codex admission for chat works now**: the `ops/opbox` skill
   (`libraries/skills/ops/opbox.yaml`, grants `opbox.*`, `memory.*`,
   `chat.ask_user`) + `skills_by_role` for `org-admin`/`superadmin` gives chat
   turns effective tool grants. Until #1 is fixed the cell can't actually call
   the verbs.
5. **Frontend overlay fragility**: `docker-compose.boltrig.yml` (PAT inside,
   600 perms) is the cutover state, but something on this box recreates the
   frontend without it (drops port 8610 + `BOLTRIG_CHAT_PATS`). Fold the
   boltrig-chat env into opbox-prod's main compose or find the recreator
   (Opbox-Cron? the opbox session?).

## Operational notes

- **Owner passwords**: boltrig owner `will.lilley93@gmail.com` password is a
  shredded random — reclaim with
  `docker compose exec -e BOLTRIG_INIT_PASSWORD='<new>' kernel python -m boltrig.api.cli set-password --email will.lilley93@gmail.com`.
  Opbox demo user password was set to `demo-proof-2026-xK9` via the kernel dev
  reset flow — change it.
- **`docker network connect opbox-prod_backend boltrig-kernel-1`** is required
  after EVERY boltrig kernel recreate (or use
  `deploy/compose.opbox-link.yml`).
- **`boltrig-shadow` opbox worker** (`act_org_aabfe9…`, 10-verb scope baked at
  mint) keys the opbox adapter; scope changes require a re-mint (grant.set
  can't widen a minted key). The earlier leaked key was rotated/revoked.
- **Secrets env**: `BOLTRIG_SEAL_KEY` set in `.env` (required in prod posture).
  `OPBOX_MCP_TOKEN` = the boltrig-shadow key. `BOLTRIG_TRUST_CF_CONNECTING_IP`
  unset (not behind CF here). `BOLTRIG_ENABLE_LEGACY_RUNTIMES` removed (pi
  bridge over).
- **Opbox demo DB** was migrated by the `fix/migration-0125-users-fk` branch
  build; pipeline images from plain main will refuse to boot ("migration 125
  modified") until that branch merges. Demo kernel currently runs the
  `tier-proof-build` branch image (main + 69b8c3a2 + 09013844).
- Opbox-prod commits awaiting their push: `69b8c3a2`, `e1eb8ae6`, `ffa09640`,
  `6664586`. Their vjs ritual (route → orders → decision log → pathspec
  commit) was followed for each.
- Concurrent activity: another session is actively testing the codex lane
  (audit rows `codex-lane-debug-2`, `codex-lane-spawn-proof`) — coordinate
  #1/#2 with them before duplicating.
- Voice goes live with `XAI_API_KEY` in env + a `voice` channel registration
  (gateway README has the steps; not yet smoke-tested against xAI).

## Test entry points

- Boltrig suite: `.venv/bin/python -m pytest -q` (offline); PG-gated via
  `BOLTRIG_TEST_DATABASE_URL` against a throwaway container.
- Chat smoke (browser-realistic): frontend at `http://localhost:8610` (when
  the mapping survives) with the demo login; or `boltrig chat --token <PAT>`.
- Opbox consumption: invoke `opbox.matter.list` / `opbox.matter.create` (HITL)
  via `/v1/invoke` with the frontend PAT.
