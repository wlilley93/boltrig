# Boltrig console parity evidence — 2026-08-11

This directory retains the historical 06:41–06:42 UTC Worker comparison with
the latest Claude Design export and the seven live Figma `CURRENT SOURCE`
frames at a fixed 1440×900 viewport. Fresh source-bound evidence for the
current Worker is stored under `current/`. It is visual evidence, not a
substitute for runtime, accessibility, or backend-contract tests.

> **Current status:** the root-level 06:41–06:42 UTC captures remain historical.
> `current/capture-manifest.json` records a fresh seven-state capture at
> 2026-08-14T11:27:57.659Z, unchanged across the capture at source digest
> `5bf295d3bee50156e16e93edbc26b048e443486282a16f8d8f18a9a7ff0f9d99`.
> Six route-scoped VDS reviews are refreshed as unsigned `no_authority`
> records. They establish current coverage only: no frame sign-off, conformity
> verdict or parity claim exists. New chat remains a separately measured state
> of the shared Chat route and still lacks a non-invented frame digest. The
> latest manual proof run records the resulting passed, failed and vacuous
> populations without treating the unsigned visual review as acceptance; no
> standalone proof matrix was generated by this capture. The separate
> `DIR-0023` phone-density direction was browser-tested at 390x844 and 320x700,
> but it has no canonical phone frame and is not represented as signed parity.
> `DIR-0024` was visually inspected and regression-tested at the adjacent
> 1374x900 and 1375x900 inspector breakpoints; the governed 1440x900 Chat
> capture exercises its inline-rail state without creating sign-off authority.
> `DIR-0025` was browser-tested at 1100x900: the manual non-modal overlay left
> the 745px composer and transcript geometry unchanged, added no scrim or body
> isolation, and returned focus to its trigger on Escape. No compact-desktop
> canonical frame exists, so this does not create sign-off authority.
> `DIR-0026` was visually inspected with live workflow steps and queued turns:
> the expandable progress pill and thin queue sit immediately above the
> composer, while the completed governed capture correctly contains neither.
> This behavioral evidence does not create sign-off authority.

`figma-connected-inventory.json` is a separate live-source observation recorded
at 15:05 UTC. Authenticated Figma metadata reads resolved all seven named
`CURRENT SOURCE` nodes, including New chat `13:2`, at their natural 1440×900
size. The connector returned metadata XML rather than an authentic saved
`GET /v1/files/:key/nodes` response, so that observation cannot be substituted
for the input to `vds figma frames` and does not create a frame digest. Fresh
connector renders for all seven nodes also match the recorded target PNGs
byte-for-byte; that establishes target-image freshness, not structural-ledger
coverage or current Worker parity.

## Authority

- Archive: `Theme control and settings design (1).zip`
- Archive SHA-256: `330fd389ecb4e7485c90207e0a2eb8fc0885c16cbed01a6a3d20a0e945c5ad5b`
- Executable source: `Boltrig Console.dc.html`
- Source SHA-256: `fbc2fa7a6bb73eeca19bbec2622e24c113a45c974b8b723e738e287436ba0c4f`
- Figma file: `iVQBKFc3NpRH3zdOOxfPqr`
- Figma URL: <https://www.figma.com/design/iVQBKFc3NpRH3zdOOxfPqr>

| State | Figma node | Target export |
| --- | --- | --- |
| Chat run | `5:2` | `figma/chat-run__5-2.png` |
| New chat | `13:2` | `figma/new-chat__13-2.png` |
| Plugins | `14:2` | `figma/plugins__14-2.png` |
| Agents | `15:2` | `figma/agents__15-2.png` |
| Command palette | `16:2` | `figma/command-palette__16-2.png` |
| Call | `17:2` | `figma/call__17-2.png` |
| Settings / You | `22:2` | `figma/settings-you__22-2.png` |

Every target export is a live Figma node render at its natural 1440×900 size.
The sibling `shipped/` images are deterministic Worker fixture captures. The
`diff/` images and `metrics.json` are generated by `compare.py`.

## Review rule

Pixel deltas are measurements, not acceptance. A difference is recorded as a
VDS `deviate` verdict unless the shipped render conforms to the signed frame.
Missing backend facts are rendered as explicit unknown or unavailable values;
the static sample's spend, permissions, moods, credentials, computer-use
frames, and unsupported controls are never fabricated to make a screenshot
pass.

The connected account currently reports a Full seat in a Pro `My Files` plan,
but the connector does not expose a raw nodes response and this shell has no
`FIGMA_TOKEN` for a fresh REST capture. The earlier Code Connect and Variables
REST attempts did not produce eligible linkage evidence; `DECISION-0001` and
`DECISION-0004` record those external limitations. The frame PNGs and hashes
remain visual references, but they do not prove component-instance or
token-variable linkage.

## Reproduction

Run the deterministic visual fixture with the URLs in `capture-manifest.json`,
capture at 1440×900 after each declared settled condition, then generate the
comparison artifacts with:

```sh
/Users/williamlilley/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 \
  docs/design/evidence/2026-08-11-console-parity/compare.py
```

The historical source/build identity and screenshot SHA-256 values are recorded
in `capture-manifest.json` and `metrics.json`; VDS review records point back to
these durable project-relative paths. A new current-source binding requires a
lossless recapture and regenerated digests; these files must not be relabelled.
